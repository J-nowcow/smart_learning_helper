name: Build Executables

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # 수동 실행 가능

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller==6.3.0
    
    - name: Download ChromeDriver for Windows
      run: |
        Invoke-WebRequest -Uri "https://chromedriver.storage.googleapis.com/140.0.7339.185/chromedriver_win32.zip" -OutFile "chromedriver.zip"
        Expand-Archive -Path "chromedriver.zip" -DestinationPath "."
        Move-Item "chromedriver.exe" "chromedriver_140"
    
    - name: Build Windows executable
      run: |
        pyinstaller --onefile --windowed --name=스마트_학습_도우미 --add-data=chromedriver_140;. ktedu_gui.py
    
    - name: Upload Windows artifact
      uses: actions/upload-artifact@v4
      with:
        name: 스마트_학습_도우미_Windows
        path: dist/스마트_학습_도우미.exe

  build-macos:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller==6.3.0
    
    - name: Download ChromeDriver for macOS
      run: |
        curl -O https://chromedriver.storage.googleapis.com/140.0.7339.185/chromedriver_mac64.zip
        unzip chromedriver_mac64.zip
        mv chromedriver chromedriver_140
        chmod +x chromedriver_140
    
    - name: Build macOS executable
      run: |
        pyinstaller --onefile --windowed --name=스마트_학습_도우미 --add-data=chromedriver_140:. ktedu_gui.py
    
    - name: Upload macOS artifact
      uses: actions/upload-artifact@v4
      with:
        name: 스마트_학습_도우미_macOS
        path: dist/스마트_학습_도우미

  create-release:
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    steps:
    - name: Download Windows artifact
      uses: actions/download-artifact@v4
      with:
        name: 스마트_학습_도우미_Windows
        path: ./windows
    
    - name: Download macOS artifact
      uses: actions/download-artifact@v4
      with:
        name: 스마트_학습_도우미_macOS
        path: ./macos
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.run_number }}
        name: 스마트 학습 도우미 v${{ github.run_number }}
        body: |
          ## 📚 스마트 학습 도우미 v${{ github.run_number }}
          
          ### 🚀 사용 방법
          1. **Windows**: `스마트_학습_도우미.exe` 더블클릭
          2. **macOS**: `스마트_학습_도우미` 더블클릭
          3. 브라우저에서 온라인 강의 로그인
          4. GUI에서 "로그인 완료" 버튼 클릭
          5. 학습 시작!
          
          ### ⚠️ 주의사항
          - Chrome 브라우저가 설치되어 있어야 합니다
          - 인터넷 연결이 필요합니다
          - Python 설치가 필요하지 않습니다!
        files: |
          windows/스마트_학습_도우미.exe
          macos/스마트_학습_도우미
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
